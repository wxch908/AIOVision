<UserControl x:Class="AIOVision.ToolView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AIOVision"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="400" FontFamily="Microsoft YaHei" Loaded="ToolView_OnLoaded">
    <Border Background="White">
        <!--项目树-->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="4"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <!--流程栏-->
            <Grid Grid.Row="0">
                <local:AddProject/>
            </Grid>
            <!--分割线-->
            <GridSplitter x:Name="splitter"
                          Grid.Row="1"
                          Height="4"
                          Foreground="Red"
                          Background="Red"/>
            <!--工具栏-->
            <ScrollViewer VerticalScrollBarVisibility="Hidden"  Grid.Row="2" >
                <local:ModuleProject/>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
